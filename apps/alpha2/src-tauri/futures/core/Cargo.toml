[package]
name = "futures-core"
version = "0.1.0"
edition = "2021"
description = "核心逻辑：基于 Tick 的派生数据聚合与回调抽象"

[lib]
name = "futures_core"
crate-type = ["rlib"]

[dependencies]
futures-interfaces = { path = "../interfaces" }
chrono = { version = "0.4", default-features = true }
ordered-float = "4"