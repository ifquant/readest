[package]
name = "ctp-client"
version = "0.1.0"
edition = "2021"
description = "CTP 期货交易适配器（示例）"

[lib]
name = "ctp_client"
crate-type = ["rlib"]

[dependencies]
serde = { version = "1", features = ["derive"] }
thiserror = "1"
futures-interfaces = { path = "../interfaces" }